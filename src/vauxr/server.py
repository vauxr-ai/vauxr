"""Entry point — wires WS + HTTP servers together.

Phase 1: placeholder. Subsequent phases fill this in.
"""

from __future__ import annotations


def main() -> int:
    raise SystemExit("vauxr Python entry point not yet wired up — see build plan phases")
